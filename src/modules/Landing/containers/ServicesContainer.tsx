import React from "react";
import { AppLayout } from "@layouts";
import { ServicesScreen } from "@modules";

const ServicesContainer: React.FC = () => {
  return (
    <AppLayout>
      <ServicesScreen />
    </AppLayout>
  );
};

export default ServicesContainer;
