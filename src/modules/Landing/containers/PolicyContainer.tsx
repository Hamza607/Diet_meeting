import React from "react";
import { AppLayout } from "@layouts";
import { PolicyScreen } from "@modules";

const PolicyContainer: React.FC = () => {
  return (
    <AppLayout>
      <PolicyScreen />
    </AppLayout>
  );
};

export default PolicyContainer;
